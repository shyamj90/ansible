---
- name: Setup web server
  hosts: web
  become: yes
  tasks:
  - name: Update yum 
    yum:
      update_cache: yes

  - name: Install nginx
    yum:
      name: nginx
      state: present 

  - name: Start Nginx server
    service:
      name: nginx
      state: started
